# Profile image

Place your profile photo here as:

```text
public/images/profile.png
```

The portfolio uses `/images/profile.png`. If the file is missing, the hero displays a clean initials placeholder automatically.

# Profile image

Place your profile photo here as:

```text
public/images/profile.jpg
```

The portfolio uses `/images/profile.jpg`. If the file is missing, the hero displays a clean initials placeholder automatically.
